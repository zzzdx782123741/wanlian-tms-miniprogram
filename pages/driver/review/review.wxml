<!--pages/driver/review/review.wxml-->
<view class="review-page">
  <view class="loading-container" wx:if="{{loading}}">
    <view class="loading-spinner"></view>
    <text class="loading-text">加载中...</text>
  </view>

  <view class="review-form" wx:if="{{!loading && order && store}}">
    <view class="review-header">
      <text class="header-title">服务评价</text>
      <text class="header-subtitle">您的评价将帮助我们持续提升服务</text>
    </view>

    <view class="store-info-card" wx:if="{{store}}">
      <view class="store-name">{{store.name || '维修门店'}}</view>
      <view class="store-address" wx:if="{{store.address}}">
        <image class="address-icon svg-icon-sm" src="/images/icons/location-pin.svg" mode="aspectFit" />
        <text wx:if="{{store.address.full}}">{{store.address.full}}</text>
        <text wx:elif="{{store.address.city}}">{{store.address.city}}{{store.address.district || ''}}{{store.address.detail || ''}}</text>
        <text wx:else>{{store.address}}</text>
      </view>
    </view>

    <view class="rating-section">
      <view class="rating-item">
        <view class="rating-label">
          <image class="label-icon svg-icon-md" src="/images/icons/wrench.svg" mode="aspectFit" />
          <text class="label-text">维修技术</text>
        </view>
        <view class="rating-stars">
          <block wx:for="{{[1, 2, 3, 4, 5]}}" wx:key="index">
            <view
              class="star {{ratings.technicalSkill >= index ? 'active' : ''}} {{ratings.technicalSkill >= index + 0.5 && ratings.technicalSkill < index ? 'half' : ''}}"
              bindtap="onRateStar"
              data-field="technicalSkill"
              data-value="{{index}}"
            >
              <text class="star-icon">★</text>
            </view>
          </block>
        </view>
        <view class="rating-text">{{ratingTexts[ratings.technicalSkill]}}</view>
      </view>

      <view class="rating-item">
        <view class="rating-label">
          <image class="label-icon svg-icon-md" src="/images/icons/check-circle.svg" mode="aspectFit" />
          <text class="label-text">服务质量</text>
        </view>
        <view class="rating-stars">
          <block wx:for="{{[1, 2, 3, 4, 5]}}" wx:key="index">
            <view
              class="star {{ratings.serviceEfficiency >= index ? 'active' : ''}} {{ratings.serviceEfficiency >= index + 0.5 && ratings.serviceEfficiency < index ? 'half' : ''}}"
              bindtap="onRateStar"
              data-field="serviceEfficiency"
              data-value="{{index}}"
            >
              <text class="star-icon">★</text>
            </view>
          </block>
        </view>
        <view class="rating-text">{{ratingTexts[ratings.serviceEfficiency]}}</view>
      </view>

      <view class="rating-item">
        <view class="rating-label">
          <image class="label-icon svg-icon-md" src="/images/icons/money.svg" mode="aspectFit" />
          <text class="label-text">收费透明</text>
        </view>
        <view class="rating-stars">
          <block wx:for="{{[1, 2, 3, 4, 5]}}" wx:key="index">
            <view
              class="star {{ratings.pricingTransparency >= index ? 'active' : ''}} {{ratings.pricingTransparency >= index + 0.5 && ratings.pricingTransparency < index ? 'half' : ''}}"
              bindtap="onRateStar"
              data-field="pricingTransparency"
              data-value="{{index}}"
            >
              <text class="star-icon">★</text>
            </view>
          </block>
        </view>
        <view class="rating-text">{{ratingTexts[ratings.pricingTransparency]}}</view>
      </view>
    </view>

    <view class="tags-section">
      <view class="section-title">
        <text class="title-text">服务印象</text>
        <text class="title-hint">（多选）</text>
      </view>
      <view class="tags-container">
        <view class="tag-item {{tagsStatus['技术专业'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="技术专业">技术专业</view>
        <view class="tag-item {{tagsStatus['服务热情'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="服务热情">服务热情</view>
        <view class="tag-item {{tagsStatus['完工迅速'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="完工迅速">完工迅速</view>
        <view class="tag-item {{tagsStatus['价格公道'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="价格公道">价格公道</view>
        <view class="tag-item {{tagsStatus['环境整洁'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="环境整洁">环境整洁</view>
        <view class="tag-item {{tagsStatus['响应及时'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="响应及时">响应及时</view>
        <view class="tag-item {{tagsStatus['收费透明'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="收费透明">收费透明</view>
        <view class="tag-item {{tagsStatus['值得信赖'] ? 'active' : ''}}" catchtap="onToggleTag" data-tag="值得信赖">值得信赖</view>
      </view>
    </view>

    <view class="comment-section">
      <view class="section-title">
        <text class="title-text">详细评价</text>
        <text class="title-hint">（选填，最多100字）</text>
      </view>
      <textarea
        class="comment-input"
        placeholder="说说本次服务体验，帮助我们持续改进..."
        maxlength="100"
        value="{{comment}}"
        bindinput="onCommentInput"
        auto-height
      ></textarea>
      <view class="char-count">{{comment.length}}/100</view>
    </view>

    <view class="anonymous-section">
      <view class="anonymous-option" bindtap="toggleAnonymous">
        <view class="checkbox {{anonymous ? 'checked' : ''}}">
          <text class="check-icon" wx:if="{{anonymous}}">✓</text>
        </view>
        <text class="anonymous-label">匿名评价</text>
      </view>
      <view class="anonymous-hint">匿名后将隐藏您的昵称</view>
    </view>

    <view class="submit-section">
      <button class="submit-btn" bindtap="onSubmit">提交评价</button>
      <button class="skip-btn" bindtap="onSkip">暂不评价</button>
    </view>
  </view>
</view>
